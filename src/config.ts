export const GAME_WIDTH = 1280;
export const GAME_HEIGHT = 800;

// Fonts live in @ui/uiTheme — single source of truth for the type system.

// Board dimensions — the descent is 5 chapters of 40 nodes each.
export const TOTAL_NODES = 200;
export const CHAPTERS = 5;
export const NODES_PER_CHAPTER = 40;
